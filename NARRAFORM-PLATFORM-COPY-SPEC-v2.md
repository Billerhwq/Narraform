# Narraform 平台文案生成规范 v2

状态：Draft for implementation  
版本：2026.08-v2  
适用范围：小红书、知乎回答、知乎文章、微信公众号、通用文案  
不包含：图片生成、账号登录、自动发布、排期、互动和数据分析

## 1. 规范目的

本规范定义 Narraform 如何把同一份事实和内容策略转化为不同平台的原生文案。平台适配不得只改变长度或平台名称，必须改变阅读入口、信息结构、论证深度、互动方式和输出字段。

本规范吸收开源 Skill 中可复用的内容生产经验，但不直接执行或拼接其提示词、脚本和发布流程。所有外部经验必须先转为 Narraform 自己的结构化规则，并接受事实安全、用户意图和产品边界约束。

## 2. 规范用语

- **MUST**：硬性要求，不满足时不得把结果标记为可交付。
- **SHOULD**：推荐要求，不满足时允许交付，但必须产生质量提醒。
- **MAY**：可选能力，由内容类型、用户要求或资料情况决定。
- **阻断错误**：不能自动补造，必须修复或询问用户。
- **可修复错误**：不改变事实和用户意图时，系统可以自动修复一次。
- **建议项**：只提示，不因“更像 AI”而阻断交付。

## 3. 规则来源与取舍

| 来源 | 吸收内容 | 不引入内容 |
|---|---|---|
| `universal-content-publisher` | 内容类型与平台分离、证据账本、事实/意见/推断/未知分层、自然表达和跨平台重组 | 发布审批、回执、平台账号和媒体执行流程 |
| `XiaohongshuSkills` | 标题加权长度上限、图文发布需媒体、最终内容需人工确认 | 登录、浏览器、互动、Cookie 和发布命令 |
| `xhs-visual-director` | 内容类型、传播目标、读者情绪、收藏价值、移动端短文本和图文节奏 | 默认审美偏好、十问流程、图片批量生成和固定视觉风格 |
| `do-anything-in-zhihu` | 回答与文章分流、回答必须绑定问题、Markdown 结构、话题数量和草稿/发布状态区分 | 浏览器操作、登录态和发布验证 |
| `zhiforge` | 先给结论、知识匹配、问题相关性、实操细节和发布前质量审查 | 固定个人 IP、站外引流、自动发布、虚构口语和故意不工整 |
| `aws-wechat-article-writing` | 目标读者、调性、结构预设、资料优先、写作与审稿分阶段 | 模型配置、仓库目录、发布方式和嵌入占位符 |
| `aws-wechat-article-topics` | 选题模式、多个角度、标题候选、摘要和大纲 | 热点联网流程、草稿目录和发布元数据 |
| `aws-wechat-article-review` | 标题、摘要、正文、事实、敏感词、AI 味和终审分级 | 微信发布专用 embed 和图片就位检查 |
| `aws-wechat-article-formatting` | Markdown 与公众号正文分离、移动阅读和标题不重复进入正文 | HTML 主题、内联样式和排版脚本 |
| `grammar-check` | 语法、逻辑、衔接、冗余、语气一致性和问题优先级 | 英文专属语法规则的直接套用 |
| `value-prop-statements` | 受众、痛点、主要收益、能力依据和使用场景的映射 | 无证据的效果承诺和通用营销套话 |

本规范只总结通用方法，不复制外部 Skill 的原文模板。规则冲突时按以下顺序处理：

```text
法律、安全与事实边界
> 用户明确要求
> 平台硬性约束
> Narraform 产品规则
> 开源 Skill 经验规则
> 模型默认判断
```

## 4. 三层生成合同

每次生成必须由三层合同编译得到：

```text
TaskBrief（写什么）
+ StrategySpec（为什么这样写）
+ PlatformSpec（在目标平台怎么写）
= RuntimeGenerationContract
```

### 4.1 TaskBrief

TaskBrief 是跨平台事实源，至少包含：

```json
{
  "taskId": "task_uuid",
  "subject": {
    "name": "主题或产品名",
    "category": "内容所属类别",
    "description": "只使用已确认信息的简述"
  },
  "contentType": "product_marketing",
  "facts": [
    {
      "factId": "fact_001",
      "statement": "可对外使用的事实",
      "sourceId": "material_001",
      "origin": "user_provided",
      "confidence": 1
    }
  ],
  "opinions": [],
  "experiences": [],
  "unknowns": [],
  "constraints": {
    "mustInclude": [],
    "mustAvoid": [],
    "unsupportedClaims": []
  }
}
```

MUST：

- 事实、作者观点、真实经历、系统推断和未知信息必须分开保存。
- 产品能力、数字、价格、日期、排名、用户评价和结果声明必须能追溯到 `factId`。
- 用户未提供真实经历时，不得生成“我用了”“亲测”“客户反馈”等表达。
- 文件名、README、本地路径、模型、提示词和内部处理过程不得进入外发文案。

### 4.2 StrategySpec

StrategySpec 是用户确认的内容策略：

```json
{
  "strategyId": "pain_solution",
  "audience": {
    "label": "目标读者",
    "origin": "inferred",
    "confidence": 0.8
  },
  "goal": "trial_conversion",
  "readerSituation": "读者遇到的具体情境",
  "coreMessage": "本篇只需记住的一句话",
  "valueProposition": {
    "pain": "读者问题",
    "benefit": "可以获得的价值",
    "capability": "支撑该价值的已确认能力",
    "evidenceFactIds": ["fact_001"]
  },
  "ctaIntent": "invite_trial",
  "forbiddenClaims": []
}
```

MUST：受众、痛点和目标如果不是用户明确提供，必须标记为 `inferred`，不得写成市场调研结论。

### 4.3 PlatformSpec

每个平台规范必须提供：

```js
{
  id,
  mode,
  version,
  supportedContentTypes,
  requiredInputs,
  outputSchema,
  recommendedLength,
  hardLimits,
  structureRules,
  titleRules,
  styleRules,
  ctaRules,
  forbiddenPatterns,
  qualityRules,
  repairRules
}
```

## 5. 内容类型路由

内容类型和平台必须独立判断。小红书不等于产品营销，知乎也不等于长文。

| 内容类型 | 核心结构 | 关键风险 |
|---|---|---|
| `product_marketing` | 问题或场景 → 能力 → 证据 → 适用人群 → 行动 | 夸大能力、虚构效果 |
| `tutorial` | 目标 → 前置条件 → 步骤 → 验证 → 限制 | 步骤不可复现 |
| `opinion` | 判断 → 理由 → 反例或边界 → 结论 | 把意见写成事实 |
| `personal_story` | 场景 → 决策 → 变化 → 反思 | 虚构第一人称经历 |
| `case_study` | 背景 → 方法 → 证据 → 结果 → 限制 | 未授权案例和指标 |
| `event_announcement` | 对象 → 时间地点 → 规则 → 行动 | 时间、价格、资格错误 |
| `release_update` | 变化 → 用户影响 → 使用方式 → 迁移注意 | 版本和日期错误 |
| `brand_story` | 起点 → 选择 → 价值判断 → 当下 | 编造品牌历史 |
| `general_article` | 读者问题 → 信息展开 → 判断或建议 | 内容泛化、缺少重点 |

## 6. 全平台公共规范

### 6.1 事实与逻辑

MUST：

- 每项可验证声明必须来自 TaskBrief。
- 不把时间先后自动写成因果关系。
- 不用“最好、第一、唯一、百分之百、永久”等绝对表述，除非有明确、可公开的证据。
- 用户要求数字但资料没有数字时，返回补充问题或改为不含数字的表达。
- 内容修改后必须重新执行事实检查。

### 6.2 表达

MUST：

- 使用自然中文，不泄露资料来源和生成过程。
- 每个主要段落必须承载事实、判断、解释、例子或行动信息之一。
- 语气必须与目标读者和用户要求一致。
- 不为了“像人”而添加错别字、随机口癖、虚构故事、伪造情绪或故意破坏结构。

SHOULD：

- 从具体问题、场景、冲突或判断进入。
- 保留必要的条件、反例和适用边界。
- 列表只用于真正连续或需要扫描的信息。
- 删除重复总结、空洞价值词、机械过渡和无关升华。

### 6.3 AI 味诊断

AI 味属于建议性质量维度，不用于判断作者身份，也不单独阻断交付。

需要检测：

- 万能宏观开头，如“随着……发展”“在当今时代”。
- “不是 X，而是 Y”在短范围内重复出现。
- 首先、其次、最后式机械三段论。
- 段落长度和句式高度均匀。
- 面面俱到但没有重点。
- “值得一提的是、总而言之、希望对你有帮助”等模板句。
- 赋能、闭环、抓手、底层逻辑等概念密集但缺少具体信息。
- 空洞祝福、强行拔高、模板化假故事和无证据的精确数字。

自动修复时只能删除或改写命中的具体片段，不得整篇随机口语化。最多修复两轮，每轮后重新执行事实检查。

### 6.4 输出状态

- `ready`：无阻断错误，可以交付。
- `ready_with_warnings`：只有建议项或非阻断平台提醒。
- `needs_input`：缺少主体、关键事实、真实经历或必须参数。
- `blocked`：存在敏感、高风险或相互冲突且无法安全修复的内容。

## 7. 小红书文案规范

> 本节的详细、可执行规则由 `NARRAFORM-XHS-PLATFORM-SPEC-v4.md` 定义。若本节与该文件发生冲突，小红书生成、换标题、换正文、润色和自定义修改均以 v4 为准；事实安全和全平台公共规范仍以本文第 4-6 节为上位约束。

### 7.1 用户任务

优先解决快速理解、收藏、讨论或轻量转化。支持产品介绍、经验分享、教程、清单、活动、观点、探店和对比，但不得默认路由为“种草”。

### 7.2 输出合同

```json
{
  "titleCandidates": ["3-5 个不同角度的标题"],
  "bodyMarkdown": "完整正文，不含标签列表",
  "topics": ["3-8 个直接相关的话题"],
  "commentPrompt": "可选的自然互动问题"
}
```

### 7.3 长度与硬限制

- 标题推荐 12-20 个中文字符。
- 发布兼容硬限制：标题加权长度 MUST 不超过 38；中文和中文标点按 2，英文、数字和半角字符按 1。
- 正文推荐 300-800 个中文字符；用户明确要求时可以调整。
- Emoji 默认 0-4 个，专业、技术和严肃主题默认不用。
- 话题推荐 3-8 个，必须与正文直接相关，不堆泛流量词。

### 7.4 结构规则

MUST：

- 前 80 个字符建立具体场景、问题、冲突或明确判断。
- 产品营销必须把“能力”翻译成目标读者可以理解的具体作用。
- 必须说明适合谁；存在明显使用边界时也要说明不适合什么情况。
- 短段落适配手机阅读，每段通常 1-3 句。
- 正文和话题分字段输出。

SHOULD：

- 点击目标强调信息差或场景冲突。
- 收藏目标强调框架、步骤、清单和复用价值。
- 转化目标先建立需求，再自然出现产品，不从品牌自我介绍开场。
- 评论引导使用一个与正文相关、读者容易回答的问题。

### 7.5 禁止模式

- 默认使用“姐妹们、宝子们、绝绝子、闭眼冲”等装熟套话。
- 虚构亲测、购买、长期使用、探店或客户评价。
- 连续感叹号、Emoji 堆叠、泛话题堆叠。
- “看完你就懂了”“建议收藏”“错过后悔”等无内容支撑的点击诱导。
- 把产品功能列表直接改写成多段宣传口号。

### 7.6 质量门

- MUST：至少一个标题、正文、话题字段完整。
- MUST：所有产品声明可回溯到事实。
- MUST：没有虚构个人体验和绝对化效果。
- SHOULD：前两段已经解释为什么值得继续看。
- SHOULD：至少一个段落提供具体信息而不是态度表达。
- SHOULD：标题候选在角度上有差异，而不只是替换同义词。

## 8. 知乎文案规范

### 8.1 模式路由

- 用户提供具体问题、问题 URL 或明确说“回答”时，使用 `answer`。
- 用户明确说“文章、专栏、长文”时，使用 `article`。
- 信息不足时，系统可以建议模式，但不得把文章伪装成问题回答。

### 8.2 知乎回答

必需输入：`questionTitle`。问题描述和现有讨论为可选输入。

输出合同：

```json
{
  "mode": "answer",
  "questionTitle": "原问题",
  "bodyMarkdown": "回答正文",
  "topics": []
}
```

规则：

- 前 150 个字符 MUST 直接给出核心判断，不能先写宏观背景。
- 正文推荐 800-2500 个中文字符。
- 每个主要结论必须有事实、推理、例子或明确前提支撑。
- 产品内容必须先解决问题，再把产品作为方法、案例或选项引入。
- MUST 至少说明一个条件、限制、反例或不适用情况。
- MAY 使用二级标题、列表、引用和代码块；结构必须服务论证。
- 没有真实经验时不得使用“我在项目里”“踩过这个坑”等经验口吻。
- 结尾优先收束判断或邀请讨论，不默认站外引流。

### 8.3 知乎文章

输出合同：

```json
{
  "mode": "article",
  "titleCandidates": ["3 个标题候选"],
  "summary": "60-120 字摘要",
  "bodyMarkdown": "文章正文",
  "topics": ["0-5 个精确话题建议"]
}
```

规则：

- 正文推荐 1200-3500 个中文字符。
- 标题必须表达讨论对象或核心判断，不使用无证据数字和身份背书。
- 摘要必须概括价值和主要判断，不能复制正文第一段。
- 正文通常使用 3-6 个有语义的小标题，避免“什么是、为什么、怎么做、总结”的机械模板。
- 技术或教程文章应包含可执行步骤、前置条件、失败情况或验证方式。
- 观点文章应有明确取舍，但不得为了“有立场”而使用攻击性或无证据结论。
- 推荐话题最多 5 个，必须精确匹配内容主题。

### 8.4 知乎质量门

- MUST：回答模式有明确问题，文章模式有标题。
- MUST：第一屏已经回答问题或建立清晰判断。
- MUST：主要结论存在事实或逻辑支撑。
- MUST：引用、数字、案例和经历没有被补造。
- SHOULD：产品名出现前，读者已经理解讨论的问题。
- SHOULD：Markdown 层级完整，没有空标题和过度加粗。
- SHOULD：不存在连续的培训 PPT 式金句和万能平衡句。

## 9. 微信公众号文案规范

### 9.1 用户任务

适合深度阅读、品牌沟通、教程、观点、活动、更新和复盘。公众号文案生成只负责内容，不包含 HTML 排版、封面、嵌入组件和草稿箱操作。

### 9.2 输出合同

```json
{
  "titleCandidates": ["3-5 个不同角度的标题"],
  "summary": "80-128 字摘要",
  "bodyMarkdown": "不重复文章标题的正文",
  "mediaNotes": []
}
```

### 9.3 长度与结构

- 标题推荐 18-28 个中文字符；具体硬上限由接入渠道配置决定。
- 摘要推荐 80-128 个中文字符，必须与正文一致且不能复制开头。
- 正文推荐 1200-2500 个中文字符；深度文章可由用户要求扩展。
- 开头 200 个字符内 MUST 建立阅读理由、具体问题或文章判断。
- 正文通常使用 3-6 个二级标题，每节只承担一个主要任务。
- 段落以移动阅读为准，通常 3-5 行；长短可以变化，但不能整篇等长。
- 结尾必须服从策略：总结、行动、讨论或留白均可，不默认关注、点赞和转发。

### 9.4 标题规范

标题候选 SHOULD 混合不同角度，例如判断、问题、干货、故事或信息差，但不得为了凑风格编造数字、身份、故事和效果。

MUST：

- 标题和正文一致。
- 数字标题中的数字有事实依据，或只是正文真实存在的条目数量。
- 不使用“震惊、不转不是、效率提升 10 倍”等无证据套路。
- 不把“建议收藏、万字长文、看完就懂”作为默认吸引方式。

### 9.5 正文规范

- 产品文章必须解释能力如何作用于真实任务，不能只罗列功能。
- 教程必须给出目标、前置条件、步骤和限制。
- 观点文章必须有主要判断和取舍重点。
- 活动文章必须核对时间、地点、参与规则、价格和行动路径。
- 复盘和案例必须区分已发生事实、作者判断和未验证推断。
- 图片需求只输出 `mediaNotes`，不得用图片占位符污染纯文案结果。

### 9.6 质量门

- MUST：标题、摘要、正文完整。
- MUST：标题、摘要和正文表达一致但不机械重复。
- MUST：敏感词、禁用词、事实、数据和来源通过检查。
- SHOULD：每个章节有明确作用，删除不影响全文的章节应视为冗余。
- SHOULD：没有万能开头、空洞祝福、强行升华和概念堆叠。
- SHOULD：AI 味只做逐处诊断，不以“检测分数”证明作者身份。

## 10. 通用文案规范

### 10.1 路由

通用文案必须先识别用途：产品简介、活动通知、官网说明、社群通知、销售辅助、摘要、润色、改写或其他明确场景。

### 10.2 输出合同

```json
{
  "purpose": "product_intro",
  "bodyMarkdown": "结果正文",
  "alternatives": []
}
```

### 10.3 规则

- 推荐 100-800 个中文字符，服从用户明确长度要求。
- 不自动添加小红书话题、知乎回答结构、公众号章节或平台口癖。
- 润色默认小改，保留原观点、术语、事实、顺序和作者声音。
- 摘要只能压缩原文，不增加新事实和判断。
- 营销文案必须建立“目标受众 → 痛点 → 收益 → 支撑能力 → 行动”的关系。
- “专业”表示准确、清楚和信息密度高，不表示增加官话。
- “更自然”表示减少模板句、抽象词和重复，不表示自动加入网络用语。
- 用户未要求营销时，不添加购买、关注、私信或转化 CTA。

### 10.4 质量门

- MUST：完成用户指定用途，不混入其他平台格式。
- MUST：润色和摘要不改变事实含义。
- MUST：逻辑无明显矛盾、因果跳跃和指代不清。
- SHOULD：语法、标点、冗余、衔接和语气一致。
- SHOULD：只修复明确问题，不把用户文风统一成平台默认文风。

## 11. 质量规则模型

每条规则必须可结构化保存：

```json
{
  "ruleId": "xhs-title-weighted-length",
  "platform": "xiaohongshu",
  "phase": "post_generation",
  "category": "title",
  "severity": "blocking",
  "checkType": "weighted_length",
  "parameters": {
    "field": "titleCandidates",
    "max": 38
  },
  "autoFix": true,
  "source": {
    "type": "contentflow",
    "version": "2026.08-v2"
  }
}
```

质量检查顺序：

1. JSON 输出结构检查。
2. 事实、数字、经历和敏感声明检查。
3. 平台硬限制检查。
4. 内容结构和营销策略执行检查。
5. 语法、逻辑、衔接和冗余检查。
6. AI 味和风格建议检查。

## 12. 自动修复边界

系统 MAY 自动修复：

- 标题或正文字数超出建议范围。
- 话题数量不合理或与正文重复。
- Markdown 层级、空标题和重复标题。
- 模板句、重复段落、机械过渡和标点堆叠。
- 摘要复制标题或正文第一段。

系统 MUST NOT 自动补造：

- 产品功能、价格、日期、指标和排名。
- 第一人称经历、客户案例、评价和对话。
- 活动资格、优惠条件和法律结论。
- 用户未确认的受众研究结论。

发现上述缺失时，返回 `needs_input`，并且一次最多提出两个关键问题。

## 13. 跨平台改写

跨平台改写必须保留：

- TaskBrief 事实集合。
- 用户确认的核心观点。
- StrategySpec 的目标和核心信息。
- 用户明确要求保留的句子。

必须重新生成：

- 标题、开头、信息顺序、段落长度、论证深度、CTA 和平台字段。
- 不得只缩短、扩写或替换平台名称。
- 跨平台相似度过高时产生警告，并重写结构而不是替换同义词。

## 14. 后端实现映射

当前项目后续实现应按以下边界调整：

| 组件 | 职责 |
|---|---|
| `task-understanding.js` | 生成 TaskBrief、区分事实与推断、提出必要问题 |
| `strategy-engine.js` | 生成 3 个策略候选并保存用户选择 |
| `platform-specs.js` | 保存本规范的机器可执行字段和版本 |
| `content-engine.js` | 编译 RuntimeGenerationContract 并调用模型 |
| `quality.js` | 执行硬规则、建议规则和自动修复 |
| `store.js` | 保存 taskId、strategyId、specVersion 和生成版本 |

`/api/generate` 必须接收：

```json
{
  "taskId": "task_uuid",
  "strategyId": "strategy_uuid",
  "platform": "xiaohongshu",
  "platformMode": null,
  "tone": "自然、专业",
  "revisionInstruction": null
}
```

生成结果必须记录：

```json
{
  "taskId": "task_uuid",
  "strategyId": "strategy_uuid",
  "platform": "xiaohongshu",
  "specVersion": "2026.08-v2",
  "factIds": ["fact_001"],
  "qualityReport": {},
  "provider": "deepseek"
}
```

## 15. 最低验收用例

1. 同一产品资料分别生成小红书、知乎回答和公众号时，事实一致但结构明显不同。
2. 用户只说“帮我宣传一下”且没有主体或事实时，返回 `needs_input`。
3. 用户要求“亲测推荐”但没有真实经历时，阻断并提供客观介绍选项。
4. 用户要求具体增长数据但资料没有数字时，不生成数字。
5. 小红书标题加权长度超过 38 时自动修复。
6. 知乎回答没有问题标题时不能进入生成。
7. 公众号摘要复制正文第一段时自动重写摘要。
8. 通用润色不增加 CTA、话题和平台口癖。
9. “更自然”不会添加错别字、网络黑话或虚构故事。
10. 每次自动修复后重新执行事实检查，并保存相同 `factIds`。

## 16. V2 后续扩展

- 规则注册表从 JavaScript 常量迁移为版本化 JSON 或 YAML。
- 增加品牌 VoiceSpec，但不能覆盖事实和平台硬限制。
- 增加图片与正文关系的 MediaSpec，不与纯文案输出混合。
- 引入官方平台规则的定期人工复核，开源 Skill 只作为补充来源。
- 基于人工编辑差异和发布后数据调整建议规则，不自动学习未经审核的违规表达。
